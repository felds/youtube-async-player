{
  "name": "@felds/youtube-async-player",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "format": "prettier --write 'src/**/*.ts'",
    "prepare": "npm run build",
    "prebuild": "rimraf dist/*",
    "build": "tsc --build"
  },
  "precommit": [
    "format",
    "test"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/youtube": "0.0.38",
    "jest": "^24.9.0",
    "pre-commit": "^1.2.2",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.0",
    "ts-jest": "^24.2.0",
    "typescript": "^3.7.2"
  },
  "dependencies": {}
}
